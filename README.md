# 🎶 Wrapped Party Data Collection App

A Flask app that lets friends log in with Spotify, grabs their top tracks, and saves them for future analysis and party festivities.

## 🚀 Setup

1. Clone the repo:
   ```bash
   git clone https://github.com/yourusername/spotify-wrapped-party.git
   cd spotify-wrapped-party

## Repo Structure
spotify-wrapped-party/
│── app.py                # main Flask app
│── requirements.txt      # Python dependencies
│── .gitignore            # ignore secrets, env, etc.
│── README.md             # setup + usage instructions
│── data/                 # (optional) local storage for CSVs
